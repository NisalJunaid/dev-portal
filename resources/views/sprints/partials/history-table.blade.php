<section class="sprint-panel p-3"><div class="text-sm font-black mb-2">Sprint History</div><table class="sprint-table w-full text-sm"><thead><tr><th>Name</th><th>Client</th><th>Ended</th></tr></thead><tbody>@foreach($sprints as $sprint)<tr><td><a href="{{ route('sprints.show',$sprint) }}" class="font-bold">{{ $sprint->name }}</a></td><td>{{ $sprint->client?->name }}</td><td>{{ optional($sprint->ended_at)->format('M j, Y') }}</td></tr>@endforeach</tbody></table><div class="mt-2">{{ $sprints->links() }}</div></section>
